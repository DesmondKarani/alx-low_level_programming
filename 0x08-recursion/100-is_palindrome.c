#include "main.h"
/**
 * is_palindrome -  a function that returns 1 if a string is a palindrome
 *	and 0 if not.
 * @s: The string to be assessed.
 * Return: Returns 1 if string is empty, 0 otherwise.
 */
int is_palindrome(char *s)
{
	if (*s == '\0')
		return (1);
	else
		return (0);
}
