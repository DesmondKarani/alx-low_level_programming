#include <string.h>
#include "main.h"
/**
 * _strcmp - a function that compares two strings.
 *
 *@s1: the string to be compared.
 *@s2: the string to be compared.
 *Return: returns the results of the compared strings.
 */
int _strcmp(char *s1, char *s2)
{
	return (s1, s2);
}
